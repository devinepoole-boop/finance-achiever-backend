// EMERGENCY STARTUP - Skip all imports that might cause memory leaks
import './ultra-minimal';

console.log('[EMERGENCY] Ultra-minimal server activated');
console.log('[EMERGENCY] All features disabled to prevent memory leak');
console.log('[EMERGENCY] Only health endpoint available');